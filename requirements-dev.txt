# MindForge 开发/CI 依赖锁定文件（测试与安全扫描工具）
#
# 安装方式：
#   pip install -r requirements-dev.txt
#
# 说明：固定测试与安全扫描工具的版本，避免数月后 CI 因工具链漂移而意外失败。
pytest==8.3.4
pytest-cov==6.0.0
bandit==1.8.0
pip-audit==2.7.3

# 性能基准与 PDF 报告（benchmarks/）
psutil==7.2.2
# matplotlib 3.11 要求 Python>=3.11；仅用于生成 PDF 性能报告（benchmarks/），
# 运行时代码与 tests/ 均不导入。加环境标记后 3.9/3.10 CI 可正常安装并运行测试，
# 需生成报告时在 Python 3.11+ 环境执行（仍使用此锁定版本）。
matplotlib==3.11.2; python_version >= "3.11"
reportlab==5.0.1
